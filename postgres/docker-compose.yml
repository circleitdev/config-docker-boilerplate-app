services:
    cit-postgres:
        image: postgres:17
        container_name: cit-postgres
        security_opt:
            - no-new-privileges:true
        volumes:
            - cit-postgres-data:/var/lib/postgresql/data
            - ./backups/init.sql:/docker-entrypoint-initdb.d/init.sql
            - /etc/localtime:/etc/localtime:ro
            - /etc/timezone:/etc/timezone:ro
            - ./backups:/backups
        networks:
            - cit-postgres-network
        env_file: .env
        restart: unless-stopped
        command: ['postgres']
        init: true
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB']
            interval: 30s
            timeout: 5s
            retries: 3
            start_period: 5s

volumes:
    cit-postgres-data:
        name: cit-postgres-data

networks:
    cit-postgres-network:
        name: cit-postgres-network
